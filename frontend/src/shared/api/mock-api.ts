/**
 * Раньше тут жил mockApi на localStorage. После миграции на реальный бэкенд
 * мы просто реэкспортируем настоящий API под старым именем — чтобы пейджи
 * и виджеты, которые импортят `mockApi`, не пришлось переписывать.
 *
 * Когда будет время — переименовать импорты на realApi и удалить этот файл.
 */
export { realApi as mockApi } from "./api";
export type { RealApi as MockApi } from "./api";
